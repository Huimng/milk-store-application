@page
@model MilkStore.Pages.MessageBoxManager.EditModel

<h4>MessageGroup</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MessageGroup.MessageGroupId" />
            <div class="form-group">
                <label asp-for="MessageGroup.GroupName" class="control-label"></label>
                <input asp-for="MessageGroup.GroupName" class="form-control" />
                <span asp-validation-for="MessageGroup.GroupName" class="text-danger"></span>
            </div>
            @* <div class="form-group">
                <label asp-for="MessageGroup.CustomerId" class="control-label"></label>
                <select asp-for="MessageGroup.CustomerId" class="form-control" asp-items="ViewBag.CustomerId"></select>
                <span asp-validation-for="MessageGroup.CustomerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MessageGroup.ManagerId" class="control-label"></label>
                <select asp-for="MessageGroup.ManagerId" class="form-control" asp-items="ViewBag.ManagerId"></select>
                <span asp-validation-for="MessageGroup.ManagerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MessageGroup.Status" class="control-label"></label>
                <select asp-for="MessageGroup.Status" class="form-control"></select>
                <span asp-validation-for="MessageGroup.Status" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
